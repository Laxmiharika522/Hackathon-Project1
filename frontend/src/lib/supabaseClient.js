// Supabase client — will be re-enabled when Supabase auth is added back.
// For now, auth uses simple localStorage (see context/AuthContext.jsx).
//
// To re-enable:
// 1. npm install @supabase/supabase-js
// 2. Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY to .env
// 3. Uncomment the code below and update AuthContext.jsx

/*
import { createClient } from '@supabase/supabase-js'
const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL
const SUPABASE_ANON_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY
export const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY)
*/
